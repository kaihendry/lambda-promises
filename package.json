{
  "name": "lambda-promise-tests",
  "author": "Kai Hendry <hendry@iki.fi>",
  "private": true,
  "scripts": {
    "lint": "eslint --ext .js .",
    "testr": "serverless invoke -f promise",
    "test": "serverless invoke local -f promise",
    "deploy": "serverless deploy",
    "logs": "sls logs -f promise"
  },
  "dependencies": {
    "eslint": "^3.14.1",
    "eslint-config-airbnb-base": "^11.0.1",
    "eslint-plugin-import": "^2.2.0"
  },
  "devDependencies": {
    "node-uuid": "^1.4.7"
  }
}
